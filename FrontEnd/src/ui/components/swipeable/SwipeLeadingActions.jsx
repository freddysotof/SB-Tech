import {
    LeadingActions
} from 'react-swipeable-list';

export const SwipeLeadingActions = ({ children }) => {
    return (
        <LeadingActions>
            {children}
        </LeadingActions>
    )
}


